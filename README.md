# TP Bot Discord
DESTREZ Corentin

----
## Compile binaries
A makefile is already created in the src/ directory.
Make sure to replace **TOKEN_BOT** with your bot token in the makefile.

----
## Usage
Execute the make file in a terminal located in src/.
